<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.excean.vphone.middleware">

    <application>
        <activity android:name="com.excean.middleware.ui.base.WebViewActivity" />

        <meta-data
            android:name="MainChId"
            android:value="${mainChId}" />
        <meta-data
            android:name="SubChId"
            android:value="${subChId}" />
    </application>
</manifest>